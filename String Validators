if __name__ == '__main__':
    s = input()
    has_alnum = has_alpha = has_digit = has_lower = has_upper = False
    
    for ch in s:
        if ch.isalnum():
            has_alnum = True
        if ch.isalpha():
            has_alpha = True
        if ch.isdigit():
            has_digit = True
        if ch.islower():
            has_lower = True
        if ch.isupper():
            has_upper = True
    
    print(has_alnum)
    print(has_alpha)
    print(has_digit)
    print(has_lower)
    print(has_upper)
